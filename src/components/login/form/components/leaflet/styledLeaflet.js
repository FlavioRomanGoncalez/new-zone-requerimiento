import { Box, styled } from "@mui/material";


export const StyledLeafletContainer = styled(Box)`
    .leaflet-container{
        height: 300px;
    }
    .leaflet-control-attribution{
        font-size: small;
        a{
            font-size: small;
        }
    }
`